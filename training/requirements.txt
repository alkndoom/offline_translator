# Dataset generation (teacher = Claude)
anthropic>=0.40
pydantic>=2.0

# QLoRA fine-tuning
torch
transformers>=4.44
datasets>=2.20
peft>=0.13
trl>=0.11
accelerate>=0.34
bitsandbytes>=0.43   # 4-bit quantization (Linux/CUDA; see README for macOS)
